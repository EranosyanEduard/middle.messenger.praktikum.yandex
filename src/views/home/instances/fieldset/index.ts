import {chatTitleOptions, factory, searchOptions} from "~/src/components/input/utils"

const [chatTitleInput, searchInput] = factory([chatTitleOptions, searchOptions])

export {chatTitleInput, searchInput}
